# Normalise to LF in the repo so the Linux Docker builds and CI see consistent files.
* text=auto eol=lf

# Binary assets
*.png binary
*.jpg binary
*.ico binary
*.pdf binary

# Generated: keep out of diffs and language stats
package-lock.json linguist-generated=true -diff
